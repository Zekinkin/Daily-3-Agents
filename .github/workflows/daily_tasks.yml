name: 🤖 每日内容集中生成 (晚间备稿)

on:
  schedule:
    # 北京时间 21:00 = UTC 13:00
    # 每天晚上 9 点，一次性备好明天的三顿饭
    - cron: '0 13 * * *'
  
  workflow_dispatch:
    inputs:
      task:
        description: '手动测试任务'
        required: true
        default: 'morning'
        type: choice
        options:
        - morning
        - afternoon
        - evening
        - all_daily

# 👇👇👇 修改点 1: 必须赋予仓库写入权限，否则无法保存 JSON 文件
permissions:
  contents: write

jobs:
  generate-all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          # 👇👇👇 修改点 2: 设置 fetch-depth 为 0，确保 Git 历史完整，防止推送报错
          fetch-depth: 0

      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: echo '${{ secrets.SERVICE_ACCOUNT_JSON }}' > service_account.json
      
      - name: 批量生成内容
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_RECIPIENTS: ${{ secrets.MAIL_RECIPIENTS }}
        run: |
          # 如果是手动触发且选了特定任务
          if [ "${{ github.event_name }}" == "workflow_dispatch" ] && [ "${{ github.event.inputs.task }}" != "all_daily" ]; then
             python main.py --task ${{ github.event.inputs.task }}
          else
             # 定时触发 (或手动选了 all_daily)：连续生成三篇
             echo "🌙 开始生成明日内容..."
             
             echo "1️⃣ 生成早报..."
             python main.py --task morning
             
             echo "2️⃣ 生成午报..."
             python main.py --task afternoon
             
             echo "3️⃣ 生成晚报..."
             python main.py --task evening
             
             echo "✅ 全部生成完毕！请查收 3 封预览邮件。"
          fi

      # 👇👇👇 修改点 3: 新增保存步骤 (Commit & Push)
      - name: 保存历史记录 (ielts_state & evening_history)
        if: success() 
        run: |
          # 1. 配置身份
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # 2. 智能添加 (关键修改：先检查文件是否存在)
          echo "📦 正在检查并添加文件..."
          
          # 如果 ielts_state.json 存在，就强制添加；否则打印跳过
          if [ -f ielts_state.json ]; then
            echo "✅ Found ielts_state.json, adding..."
            git add -f ielts_state.json
          else
            echo "⚠️ ielts_state.json not found, skipping."
          fi
          
          # 如果 evening_history.json 存在，就强制添加；否则打印跳过
          if [ -f evening_history.json ]; then
            echo "✅ Found evening_history.json, adding..."
            git add -f evening_history.json
          else
            echo "⚠️ evening_history.json not found, skipping."
          fi
          
          # 3. 添加其他可能的变化 (比如 dispatcher 可能产生的日志等)
          git add .
          
          # 4. 提交并推送
          # 只有当暂存区有东西时才提交，防止报错 "nothing to commit"
          if git diff-index --quiet HEAD; then
            echo "🛑 No changes to commit."
          else
            echo "🚀 Committing and pushing changes..."
            git commit -m "🤖 Auto-update history logs [skip ci]"
            git push
          fi
